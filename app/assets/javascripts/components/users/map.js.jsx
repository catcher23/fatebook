(function(root) {
  'use strict';

  root.UserMap = React.createClass({


    render: function () {
      return (
        <div>
          UserMap Placeholder
        </div>
      );
   }
  });
}(this));
